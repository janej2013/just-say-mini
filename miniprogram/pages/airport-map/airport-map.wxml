<view class="container" catchtap="handleBgClick">
  <!-- Background Elements -->
  <view class="bg-elements">
    <!-- Soft Clouds -->
    <view class="cloud cloud-1"></view>
    <view class="cloud cloud-2"></view>
    <view class="cloud cloud-3"></view>
    
    <!-- Thematic Icons -->
    <image class="icon-cloud icon-cloud-1" src="/assets/images/level_task_map/icon-cloud.svg" mode="aspectFit"></image>
    <image class="icon-cloud icon-cloud-2" src="/assets/images/level_task_map/icon-cloud.svg" mode="aspectFit"></image>
    <image class="icon-wind" src="/assets/images/level_task_map/icon-wind.svg" mode="aspectFit"></image>
    <image class="icon-plane-bg" src="/assets/images/level_task_map/icon-plane.svg" mode="aspectFit"></image>
    
    <!-- Control Tower -->
    <image class="control-tower-img" src="/assets/images/level_task_map/img-control-tower.svg" mode="aspectFit"></image>
    
    <!-- Ground Hint -->
    <view class="ground-hint"></view>
  </view>
  <!-- Map Content -->
  <view class="map-content">
    <!-- SVG Path Background -->
    <image class="map-path-bg" src="/assets/images/level_task_map/bg-map-path.svg" mode="aspectFill"></image>

    <!-- Level Nodes -->
    <block wx:for="{{airportData.levels}}" wx:key="levelId" wx:for-index="index">
      <view 
        class="level-node-wrapper" 
        style="top: {{index === 0 ? 580 : index === 1 ? 340 : 100}}px; left: {{index === 1 ? 240 : 40}}px;"
        catchtap="handleLevelClick"
        data-index="{{index}}"
      >
        <!-- Icon Circle -->
        <view class="node-circle {{item.status}} {{activeLevelIndex === index ? 'selected' : ''}}">
          <image 
            class="node-icon {{index === 0 ? 'plane-rotate' : ''}}" 
            src="{{index === 0 ? '/assets/images/level_task_map/icon-plane.svg' : index === 1 ? '/assets/images/level_task_map/icon-map.svg' : '/assets/images/level_task_map/icon-luggage.svg'}}" 
            mode="aspectFit"
          ></image>
          
          <!-- Selected Ring -->
          <view wx:if="{{activeLevelIndex === index}}" class="selected-ring"></view>
        </view>

        <!-- Star Rating -->
        <view class="stars-container">
          <block wx:for="{{starArray}}" wx:key="*this" wx:for-item="star">
            <image 
              class="star-icon {{star <= item.stars ? 'star-filled' : item.status === 'active' && star > item.stars ? 'star-active-empty' : 'star-empty'}}" 
              src="/assets/images/level_task_map/icon-star.svg" 
              mode="aspectFit"
            ></image>
          </block>
        </view>
      </view>

      <!-- Task Bubble -->
      <view 
        wx:if="{{activeLevelIndex === index}}" 
        class="task-bubble"
        style="{{bubbleStyle}}"
        catchtap="handleBubbleClick"
      >
        <view class="bubble-content">
          <view class="bubble-header">
            <text class="bubble-title">{{item.title.zh}}</text>
          </view>

          <!-- Task List -->
          <view class="task-list">
            <block wx:for="{{item.tasks}}" wx:key="taskId" wx:for-item="task">
              <view class="task-item">
                <view class="task-dot {{item.status === 'completed' ? 'dot-completed' : 'dot-active'}}"></view>
                <text class="task-text">{{task.zh}}</text>
              </view>
            </block>
          </view>

          <!-- Start Button -->
          <button 
            class="start-btn {{item.status === 'locked' ? 'btn-locked' : ''}}" 
            disabled="{{item.status === 'locked'}}"
            catchtap="onStart"
          >
            <text>{{item.status === 'locked' ? 'Locked' : 'Start'}}</text>
            <image wx:if="{{item.status !== 'locked'}}" class="play-icon" src="/assets/images/level_task_map/icon-play.svg" mode="aspectFit"></image>
          </button>
        </view>

        <!-- Bubble Arrow -->
        <view class="bubble-arrow {{bubbleArrowClass}}"></view>
        
        <!-- Close Button -->
        <view class="close-btn" catchtap="handleCloseBubble">
          <image class="icon-close" src="/assets/images/level_task_map/icon-close.svg" mode="aspectFit"></image>
        </view>
      </view>

      <!-- Avatar on Active Level -->
      <view 
        wx:if="{{item.status === 'active'}}" 
        class="avatar-wrapper"
        style="top: {{index === 0 ? 535 : index === 1 ? 295 : 55}}px; left: {{index === 1 ? 290 : 90}}px;"
      >
        <view class="avatar-circle">
          <image class="paw-icon" src="/assets/images/level_task_map/icon-paw.svg" mode="aspectFit"></image>
        </view>
      </view>

    </block>
  </view>
</view>
